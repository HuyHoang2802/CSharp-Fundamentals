<Window x:Class="WpfApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp.Views"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Background>
        <LinearGradientBrush StartPoint="1,1" EndPoint="1,0">
            <GradientStop Color="Gold" Offset="3"/>
            <GradientStop Color="Black" Offset="0"/>
        </LinearGradientBrush>
    </Window.Background>

    <Grid Margin="20,78,20,57">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Login Window" Grid.Row="0" Foreground="Gold" Margin="0,10,0,5" HorizontalAlignment="Center"
                   FontSize="40" FontWeight="ExtraBold"/>

        <TextBlock Text="Email:" Grid.Row="1" Foreground="Gold" Margin="0,10,0,5" FontWeight="Bold"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="2" Height="30" VerticalContentAlignment="Center" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="Mật khẩu:" Grid.Row="3" Foreground="Gold" Margin="0,10,0,5" FontWeight="Bold"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="4" Height="30" VerticalContentAlignment="Center" PasswordChanged="PasswordBox_PasswordChanged"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Đăng nhập" Width="100" Margin="0,0,10,0"  Background="Gold" FontWeight="Bold"  Command="{Binding LoginCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Button Content="Thoát" Width="100"  Background="Gold" FontWeight="Bold" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
