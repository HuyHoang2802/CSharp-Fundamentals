<UserControl x:Class="WpfApp.Views.ReportManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp.Views"
             xmlns:converters="clr-namespace:WpfApp.Converters"
             mc:Ignorable="d" >


    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="Padding" Value="5,2"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="GridLinesVisibility" Value="All"/>
            <Setter Property="BorderBrush" Value="Yellow"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FFFBEFB4"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#5C4A00"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderBrush" Value="Yellow"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        
        <converters:DateOnlyToDateTimeConverter x:Key="DateOnlyConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <DatePicker SelectedDate="{Binding StartDate, Mode=TwoWay, Converter={StaticResource DateOnlyConverter}}" Width="150" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding EndDate, Mode=TwoWay, Converter={StaticResource DateOnlyConverter}}" Width="150"/>
            <Button Content="Lọc" Command="{Binding LoadBookingCommand}" Width="Auto" Margin="0,0,10,0"/>
            <Button Content="Xuất CSV" Command="{Binding ExportReportCommand}" Width="Auto" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding DisplayError}" Foreground="Red" Background="Transparent" FontSize="12"/>
        </StackPanel>

        <!-- Customers DataGrid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding BookingDetails}" AutoGenerateColumns="False" Foreground="Black" CanUserAddRows="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ngày đặt" Binding="{Binding BookingReservation.BookingDate, StringFormat=dd/MM/yyyy}" Width="*"/>
                <DataGridTextColumn Header="Tổng tiền" Binding="{Binding BookingReservation.TotalPrice}" Width="*"/>
                <DataGridTextColumn Header="Tên khách hàng" Binding="{Binding BookingReservation.Customer.CustomerFullName}" Width="*"/>
                <DataGridTextColumn Header="Ngày Bắt đầu" Binding="{Binding StartDate, StringFormat=dd/MM/yyyy}" Width="*"/>
                <DataGridTextColumn Header="Ngày kết thúc" Binding="{Binding EndDate, StringFormat=dd/MM/yyyy}" Width="*"/>
                <DataGridCheckBoxColumn Header="Trạng thái" Binding="{Binding BookingReservation.BookingStatus}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>